<UserControl x:Class="DBInStudio.Desktop.View.ImportModeSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DBInStudio.Desktop.View"
            xmlns:res="clr-namespace:DBInStudio.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <RadioButton Content="{res:ResMarker Replace}" IsChecked="True" HorizontalAlignment="Center" Margin="0,0,0,0" GroupName="gg" VerticalAlignment="Center"/>
        <RadioButton Content="{res:ResMarker ReplaceAll}" IsChecked="{Binding IsReplaceMode,Mode=TwoWay}" HorizontalAlignment="Center" GroupName="gg" VerticalAlignment="Center" Grid.Column="1"/>
        <RadioButton Content="{res:ResMarker Append}"  IsChecked="{Binding IsAddMode,Mode=TwoWay}" GroupName="gg" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" Grid.Column="2"/>
    </Grid>
</UserControl>
